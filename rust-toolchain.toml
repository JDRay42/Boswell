# Pins the toolchain so every contributor and CI run uses the same Rust, and so
# CI matches a local install rather than floating ahead of it. `rust-version` in
# Cargo.toml declares the minimum supported version separately; bump this pin
# deliberately when you want to move to a newer compiler.
[toolchain]
channel = "1.98.0"
components = ["rustfmt", "clippy"]
